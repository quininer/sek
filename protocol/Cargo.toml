[package]
name = "sek-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = [ "derive" ] }
serde_bytes = { version = "0.11", default-features = false }
jiff = { version = "0.2", default-features = false, features = [ "serde" ] }
